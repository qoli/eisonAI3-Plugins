{
  "name": "eisonai3-plugin-protocol",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "validate": "node scripts/validate-plugin.mjs",
    "validate:registry": "node scripts/validate-registry.mjs",
    "check": "npm test && npm run validate:registry"
  }
}
